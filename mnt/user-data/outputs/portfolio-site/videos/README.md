Put your video files here (.mp4 works everywhere).

Naming used by the starter pages:
- project-1-01.mp4 → first video on the Project One page

Keep clips short and compressed (GitHub has a 100MB per-file limit,
and anything over ~20–30MB will feel slow to load). If a video is large,
consider trimming it or exporting at a lower bitrate/resolution.
